# 四半期/通期データ表示仕様（J-Quants + 株探併存）

## 1. 目的
- J-Quants `/fins/summary` の生JSONから、`Sales→FSales→NxFSales` および `EPS→FEPS→NxFEPS` の流れを、
  **データ層 / ドメイン層 / GUI層** で責務分離して扱う。
- 会社予想（今期）と来期予想の比較を、最新四半期の進捗と同時に可視化する。
- 株探保存HTMLの通期（実績/予想）を J-Quants 表示に補完統合し、GUIで併存表示する。

## 2. 現在の表示（2026-05-21 時点）

現在の出力はテキストブロックで、J-Quantsベースの主要指標・実績比較・今期予想・来期予想を表示し、末尾に株探セクションを付与する構成。

### 2.1 現在の主要ブロック
1. `■指標`
2. `■実績比較（最新FY←前FY）`
3. `■今期会社予想`
4. `■EPS/PER比較（来期予想・今期末予想・直近四半期）`
5. `■売り上げ予想(翌FY)`
6. `■株探 業績推移（通期）`（ソース注記付き）

### 2.2 現在の株探表示仕様
- カラム: `売上高 / 営業利益 / 経常利益 / 最終利益`
- 行: `実績(1行) / 今期予想 / 来期予想`
- 取得失敗時は N/A 3行。

（実装参照: `build_fundamental_output_text_impl`, `build_kabutan_forecast_output`）

## 3. 現在のデータ成形フロー（実装準拠）

### 3.1 データ層（取得）
1. `JQuantsClient.get_summary(code)` で `/fins/summary` を取得。
2. キャッシュ経由で `summary_rows: list[dict]` として返す。

### 3.2 ドメイン層（期間正規化）
1. 1行ずつ `period_record` 相当へ変換（年度・期間種別 1Q/2Q/3Q/FY を決定）。
2. 同一年度・同一期間の重複は、開示日時で新しい行を優先。
3. 欠損補完が必要な場合は行マージ（片方にしかないキーを補完）。
4. `periods` に集約：
   - `latest_fy`, `prev_fy`, `latest_quarter`
   - `current_forecast`（今期予想アンカー）
   - `next_forecast`（来期予想アンカー）

### 3.3 ドメイン層（メトリクス計算）
- 取得優先順を決めて forecast 値を抽出：
  - 今期予想: `latest_quarter -> current_forecast -> latest_fy`
  - 来期予想: `next_forecast -> current_forecast -> latest_fy`
- キー吸収（短縮/表記ゆれ）:
  - 売上: `FSales`, `Fsales`, `NxFSales`, `NxFsales`
  - EPS: `FEPS`, `NxFEPS`
- YoY計算:
  - `forecast_sales_yoy = YoY(FSales, Sales)`
  - `forecast_eps_yoy = YoY(FEPS, EPS)`
  - `next_sales_yoy = YoY(NxFSales, FSales)`
  - `next_eps_yoy = YoY(NxFEPS, FEPS)`

### 3.4 GUI層（表示）
- ドメインの `metrics` を表示用テキストに整形。
- 生JSONキーには直接依存せず、`metrics` / `periods` のみ参照。

## 4. 提案する最終出力イメージ（併存版）

方針: **J-Quants を主表示、株探を補完表示**。ただし「ソース別セクション」ではなく「指標別セクション」で表示し、セル単位でソースを見せる。

### 4.1 画面イメージ（テキストUI）

```text
【銘柄】◯◯◯◯ (1234)

■指標
株価：1,234円 / PER：12.34 / PBR：1.23
業種：◯◯　時価総額：1,234.5億円(中型主役)
配当利回り：2.10%(配当性向 35.00%)

■実績比較（最新2025年FY←2024年FY）
売上高：3,000.0億円(YoY +8.0%)←2,777.8億円
営業利益：300.0億円(YoY +10.0%)←272.7億円
純利益：200.0億円(YoY +9.5%)←182.6億円
EPS：120円(YoY +9.1%)←110円

■予想チェーン（通期） [基準年: 2026 / 判定: 決算前]
売上：実績 2024 2,778億[JQ] -> 実績 2025 3,000億[JQ] -> 予想 2026 3,200億[KBT] -> 予想 2027 3,450億[KBT]
営業益：実績 2024 273億[JQ] -> 実績 2025 300億[JQ] -> 予想 2026 340億[KBT] -> 予想 2027 380億[KBT]
最終益：実績 2024 183億[JQ] -> 実績 2025 200億[JQ] -> 予想 2026 230億[KBT] -> 予想 2027 260億[KBT]
修正1株益：実績 2024 110円[JQ] -> 実績 2025 120円[JQ] -> 予想 2026 132円[KBT] -> 予想 2027 145円[KBT]
配当：実績 2024 40円[JQ] -> 実績 2025 45円[JQ] -> 予想 2026 48円[KBT] -> 予想 2027 52円[KBT]

注記: 株探ソース=HTML（7203.html） / J-Quants欠損項目は株探で補完
```

### 4.2 決算後パターン（2026年）
- 実績: 2024, 2025, 2026
- 予想: 2027

### 4.3 決算前パターン（2026年）
- 実績: 2024, 2025
- 予想: 2026, 2027

## 5. 次実装: 表示仕様

## 5.1 表示ブロック
1. **四半期サマリ（最新四半期）**
   - 対象期間ラベル（例: `2025年度 3Q（2025-04-01〜2025-12-31 / 開示 2026-02-10 15:00）`）
   - 売上進捗（実績/通期予想）
   - 営業利益進捗（実績/通期予想）

2. **予想チェーン（Sales/EPS）**
   - 売上: `実績FY -> 今期予想(FSales) -> 来期予想(NxFSales)`
   - EPS: `実績FY -> 今期予想(FEPS) -> 来期予想(NxFEPS)`
   - 各矢印にYoYを併記。

3. **データソース注記**
   - `forecast_source_label` を明示（どの期間行から予想値を採用したか）。

## 5.2 表示フォーマット（テキスト）
- 売上チェーン（例）:
  - `売上チェーン: 3,000億円 -> 3,300億円 (YoY +10.0%) -> 3,630億円 (YoY +10.0%)`
- EPSチェーン（例）:
  - `EPSチェーン: 120.0円 -> 132.0円 (YoY +10.0%) -> 145.2円 (YoY +10.0%)`
- 欠損時:
  - 値が欠損なら `N/A`。
  - YoYの分母が0/負値/欠損なら `YoY N/A`。

## 5.3 層ごとの責務
- データ層:
  - API取得とキャッシュのみ。計算禁止。
- ドメイン層:
  - 期間正規化、forecast抽出、YoY計算、進捗計算。
  - 「決算後/決算前」の判定。
  - J-Quants/株探の統合（どの値を採用したかのソース情報を保持）。
  - 表示文言は持たず、表示向けDTO（ViewModel）を返す。
- GUI層:
  - ViewModelを人間可読テキスト/表へ render。

## 5.4 命名ポリシー適用
- 取得: `fetch_summary_rows`, `get_summary`
- 計算: `calc_metrics`, `calc_yoy`
- 判定: `grade_fiscal_timing`, `rank_forecast_yoy`, `grade_summary`
- 出力生成: `build_unified_forecast_view`, `render_quarter_table`

## 5.5 受け入れ条件
- `Sales/FSales/NxFSales` と `EPS/FEPS/NxFEPS` が同一ブロックで連続表示される。
- 表示値はすべて `metrics` 由来で、GUI層で生JSONを参照しない。
- 期間ソース注記が出る。
- 欠損時にクラッシュせず `N/A` 表示になる。
- 株探の決算前/決算後ルール（2026年例）に応じ、実績/予想の年次配列が切り替わる。
- GUI層は統合済みViewModelの描画のみを担当し、採用ロジックを持たない。
